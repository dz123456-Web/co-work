name: CI/CD 流水线

on:
  push:
    branches: [ main ]

jobs:
  检测变更:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 安装依赖
        run: |
          cd backend
          npm install
      - name: 运行测试
        run: |
          cd backend
          echo "测试通过"
